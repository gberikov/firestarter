# ESP-IDF build output (generated by idf.py, do not commit)
/build/

# Active sdkconfig — holds your local Wi-Fi password, keep it local.
# Shared, non-secret build config lives in the committed sdkconfig.defaults.
/sdkconfig
sdkconfig.old
sdkconfig.defaults.local

# Component manager (downloaded dependencies)
/managed_components/
dependencies.lock

# Python caches
__pycache__/
*.pyc

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
